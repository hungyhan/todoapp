apiVersion: v1
kind: ConfigMap

metadata:
  name: todo-config
  namespace: todo-app

data:
  NODE_ENV: "production"

  PORT: "5000"

  DB_HOST: "postgres-service"
  # DB_HOST: "postgres-service"

  DB_PORT: "5432"

  DB_NAME: "todo"
